{{> header}}

<div class="card">
  <div class="card-body">
    <h2 class="card-title text-success">Request Submitted!</h2>
    <p class="card-text">Your account request has been received and is being processed.</p>
    <hr>
    <p class="mb-0">You will receive an email notification with a validation link.</p>
  </div>
</div>

<div class="card mt-4">
  <div class="card-body">
    <h4 class="card-title">Validate Your Email</h4>
    <p>If you've already received the email, you can enter your validation token here:</p>
    <form method="POST" action="/request/validate">
      <div class="form-group">
        <label for="token">Validation Token:</label>
        <input type="text" class="form-control" id="token" name="token" required>
      </div>
      <button type="submit" class="btn btn-success mt-2">Validate Email</button>
    </form>
  </div>
</div>

<div class="text-center mt-4">
  <a href="/request/" class="btn btn-primary">Return to Home</a>
</div>

{{> footer}} 